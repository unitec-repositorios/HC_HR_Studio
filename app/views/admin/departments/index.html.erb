<div class="row">
  <div class="col-sm-12">
    <h3>Lista de Departamentos</h3>
    <div class="box box-primary">
      <div class="box-header">
        <h3 class="box-title">Lista de Departamentos</h3>
        <div class="box-tools">
          <div class="input-group input-group-sm" style="width: 90px;">
            <%= link_to "Agregar Departamento".html_safe, new_admin_department_path, :class => "btn btn-block btn-success btn-sm" %>
          </div>
        </div>
      </div>
      <!-- /.box-header -->
      <div class="box-body table-responsive no-padding">
        <table class="table table-hover">
          <tr>
            <th>Codigo</th>
            <th>Departamento</th>
            <th>Area</th>
            <th></th>
          </tr>
          <tbody>
            <%- @departments.each do |f| %>
              <% unless f.name.blank? %>
              <tr>
                <td><%= f.department_id_number %></td>
                <td><%= f.name %></td>
                <td><%= f.area.name %></td>
                <td align="right" style="width: 200px">
                  <div class="input-group input-group-xs">
                    <%= link_to edit_admin_department_path(f.id, f), class: "btn bg-blue btn-flat btn-sm" do %>
                      <i class="fa fa-pencil"></i> Editar
                    <% end %>
                      <%= link_to "Eliminar", admin_department_url(f), class: 'btn bg-red btn-flat btn-sm', method: :delete,
                        data: { confirm: 'Estas Seguro?' } %>
                  </div>
                </td>
              </tr>
              <%end%>
            <%-end%>
            </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
